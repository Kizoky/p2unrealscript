/**
 * MountedWeaponCamera
 *
 * Not really a camera, but instead we use this Actor to designate where in the
 * world the player's camera should be. Plus Actor is abstract so with this, we
 * can Spawn it.
 *
 * @author Gordon Cheng
 */
class MountedWeaponCamera extends Actor;

defaultproperties
{
    bHidden=true
}